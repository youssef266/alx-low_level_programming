#include <stdio.h>

/**
 * main - entry point
 *
 * Decription: program that prints all single digit num from 0 ,10
 *
 * Return: alwayes 0
*/

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d\n", i);
	return (0);
}
